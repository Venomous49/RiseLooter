RiseLooter overnight launch readiness

COMPLETED / VERIFIED
- Character masters: 16 canonical male/female stage assets recovered and validated at 3072x4096. master-rebuild-status.txt now reports quality_guard=passed, masters=16, dimensions=3072x4096, binary_transfer=verified.
- Female final stage: female-50-rise-looter.webp was repaired from the approved reference cell after the 16-master recovery.
- Character runtime: canonical male/female stage paths are wired; creator preview selects the canonical male/female beginner image; home/evolution art uses the selected profile gender.
- Gender silhouettes: 8 male + 8 female 4K silhouette files exist under silhouettes/male and silhouettes/female. Runtime selects the matching gender path and preserves legacy fallback only if a gender-specific asset cannot load.
- CPX postback: server-side hash validation, authenticated reward path, 70% user / 30% publisher split.
- CPX reward ledger: unique provider+transaction ID, idempotent credit, reversal handling including reversal-before-credit, service-role-only RPC.
- CPX survey bootstrap: authenticated /api/cpx/config endpoint; widget consumes server-issued ext_user_id + secure_hash when CPX_APP_SECURE_HASH is configured.
- Admin dashboard: /api/admin/summary is protected by verified Supabase session + ADMIN_USER_ID.
- Evolution test mode: visibility and access are gated through the protected admin endpoint.
- Withdrawal UI/validation: minimum aligned to 1,000 RL Coins = 10 EUR and states processing after partner commission validation.

CURRENT VERIFICATION IN PROGRESS
- The last silhouette publish commit predates the final female Rise Looter master repair. rebuild-gender-silhouettes.yml is configured to run on master changes and every 5 minutes, with a quality guard that requires the verified 16-master state. Await a newer silhouette publish/no-change validation before declaring all 16 silhouettes freshly derived from the latest masters.
- Cloudflare deploy workflow watches all 16 master files and both gender silhouette directories and also runs every 10 minutes. The repository deployment status currently records success with 16 character assets and 3072x4096 final dimensions, but the recorded source_commit predates the latest female final-stage repair. Await a newer deployment diagnostic before declaring the latest main fully confirmed live.

CPX SAFETY REVIEW
- User share is calculated as 70% and publisher share as 30% on the server and in the database ledger.
- unique(provider, transaction_id) plus row locking makes repeated callbacks idempotent.
- status=2 reversals debit previously credited RL Coins once; reversal-before-credit is persisted as terminal so late status=1 cannot credit it.
- Reward RPC execute permission is revoked from public/anon/authenticated and granted only to service_role.
- Admin summary and evolution testing require an authenticated user whose id equals ADMIN_USER_ID.

NO USER-ACTION BLOCKER
- No user action is currently required. Remaining work is verification of the scheduled silhouette rebuild and the Cloudflare deployment generated after the latest female final-stage repair, then continued small launch-readiness checks.