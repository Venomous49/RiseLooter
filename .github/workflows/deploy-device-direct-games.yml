name: Deploy device targeted games
on:
  push:
    branches: [main]
    paths: ['.github/workflows/deploy-device-direct-games.yml']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - name: Safety checks
        run: |
          set -euo pipefail
          grep -q 'offerwallgg-stable-v8.js' index.html
          grep -q 'daily-streak-ui-v1.js' index.html
          grep -q '__RL_OW_DEVICE_FILTER_V2__' offerwall-device-direct-v1.js
          grep -q 'Voir les missions' offerwall-ui-enhancements-v1.js
          grep -q 'loadLeaderboard' leaderboard-recovery-v1.js
          ! grep -q 'innerHTML=' offerwall-device-direct-v1.js
          ! grep -Eq 'setInterval|MutationObserver' offerwall-device-direct-v1.js
      - name: Wire recovery scripts
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import re
          p=Path('index.html'); s=p.read_text()
          s=re.sub(r'<script src="/offerwall-device-direct-v1\.js\?v=[^"]+" defer></script>','<script src="/offerwall-device-direct-v1.js?v=20260911-v2" defer></script>',s,count=1)
          s=re.sub(r'<script src="/offerwall-ui-enhancements-v1\.js\?v=[^"]+" defer></script>','<script src="/offerwall-ui-enhancements-v1.js?v=20260911-v5" defer></script>',s,count=1)
          tag='<script src="/leaderboard-recovery-v1.js?v=20260911-v1" defer></script>'
          if 'leaderboard-recovery-v1.js' not in s:s=s.replace('</body>',tag+'\n</body>',1)
          p.write_text(s)
          PY
          grep -q 'offerwall-device-direct-v1.js?v=20260911-v2' index.html
          grep -q 'offerwall-ui-enhancements-v1.js?v=20260911-v5' index.html
          grep -q 'leaderboard-recovery-v1.js?v=20260911-v1' index.html
      - name: Commit wiring
        run: |
          git config user.name 'RiseLooter Automation'
          git config user.email 'actions@users.noreply.github.com'
          git add index.html
          git diff --cached --quiet || git commit -m 'Restore game missions and leaderboard UI'
          git push
      - name: Deploy Cloudflare
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: npx --yes wrangler@4 deploy
      - name: Verify production
        run: |
          set -euo pipefail
          curl -fsSL 'https://loot-up.marin-iglesias.workers.dev/' -o /tmp/home
          grep -q 'offerwall-device-direct-v1.js?v=20260911-v2' /tmp/home
          grep -q 'offerwall-ui-enhancements-v1.js?v=20260911-v5' /tmp/home
          grep -q 'leaderboard-recovery-v1.js?v=20260911-v1' /tmp/home
          curl -fsSL 'https://loot-up.marin-iglesias.workers.dev/offerwall-ui-enhancements-v1.js?v=20260911-v5' | grep -q 'Voir les missions'
          curl -fsSL 'https://loot-up.marin-iglesias.workers.dev/leaderboard-recovery-v1.js?v=20260911-v1' | grep -q 'loadLeaderboard'