name: Patch targeted surveys layer
on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/patch-targeted-surveys.yml'
permissions:
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - name: Verify worker and device-format cleanup
        shell: bash
        run: |
          set -euo pipefail
          test "$(wc -l < worker.js)" -gt 500
          grep -q 'export default' worker.js
          ! grep -q '@media(max-width:1024px)' production-recovery-v3.js
          ! grep -q '@media(max-width:700px)' production-recovery-v3.js
      - name: Deploy current main to Cloudflare Workers
        shell: bash
        run: npx --yes wrangler@4 deploy
# deploy trigger device-format-cleanup-v1
